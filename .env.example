# apps/web required env vars
# Local defaults:
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# WAY Auth (origin only: no trailing slash/path/query/hash)
NEXT_PUBLIC_WAY_AUTH_BASE_URL=http://localhost:3000
WAY_AUTH_BASE_URL=http://localhost:3000
WAY_AUTH_UPSTREAM_URL=https://way-my-auth-service.vercel.app

# Vercel deployment notes:
# - Set the same keys in Vercel Project Settings -> Environment Variables.
# - Set NEXT_PUBLIC_SITE_URL to your production domain
#   (example: https://your-app.vercel.app or your custom domain).
# - Set WAY_AUTH_BASE_URL and NEXT_PUBLIC_WAY_AUTH_BASE_URL to the app origin.
# - Set WAY_AUTH_UPSTREAM_URL to the auth service origin.
